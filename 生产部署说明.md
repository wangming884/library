# 云端生产环境部署指南 (Docker Hub + 已有 MySQL)

本方案用于在**云服务器（如阿里云、腾讯云、华为云、AWS 等）**上一键拉取 Docker Hub 镜像并运行，复用服务器上已安装的 MySQL 数据库。

---

## 阶段一：在本地打包并推送到 Docker Hub

> **前提**：您需要在 [Docker Hub](https://hub.docker.com/) 注册账号，并拥有一个仓库（例如 `your-username/library-management`）。

在**本地开发机**项目根目录终端执行：

### 1. 登录 Docker Hub
```bash
docker login
# 输入您的 Docker Hub 用户名和密码 (或 Access Token)
```

### 2. 构建镜像并打标签
```bash
# 格式：docker build -t 您的用户名/仓库名:标签 .
docker build -t leemingze/library-management:v1.0.0 .
```

### 3. 推送镜像至 Docker Hub
```bash
docker push leemingze/library-management:v1.0.0
```

---

## 阶段二：云服务器数据库准备（若未初始化）

如果云服务器上的 MySQL 数据库尚未创建图书库或数据表：

1. 登录服务器 MySQL：
   ```bash
   mysql -u root -p
   ```
2. 创建数据库：
   ```sql
   CREATE DATABASE IF NOT EXISTS library_db DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
   ```
3. 导入初始表结构和预置数据：
   将项目中的 `sql/schema.sql` 和 `sql/data.sql` 上传至服务器执行：
   ```bash
   mysql -u root -p library_db < sql/schema.sql
   mysql -u root -p library_db < sql/data.sql
   ```

---

## 阶段三：云服务器部署运行

在**云端服务器**上只需保留两个配置文件即可轻量启动：

### 1. 创建应用目录
```bash
mkdir -p /opt/library && cd /opt/library
```

### 2. 上传配置文件
将以下 2 个文件上传到 `/opt/library`：
- `docker-compose.prod.yml`
- `.env.prod.example`

### 3. 创建生产环境变量文件
```bash
cp .env.prod.example .env.prod
nano .env.prod  # 或使用 vim 编辑
```

配置要点：
- 将 `DOCKER_IMAGE` 改为您在 Docker Hub 上的实际镜像名（如 `leemingze/library-management:v1.0.0`）。
- 将 `DB_PASSWORD` 改为云服务器上 MySQL 的真实密码。
- 若 MySQL 直接安装在云主机上，`DB_HOST=host.docker.internal` 即可直接访问宿主机端口；若为 RDS，则填 RDS 域名或内网 IP。

### 4. 拉取镜像并启动容器
```bash
# 拉取最新镜像
docker compose -f docker-compose.prod.yml --env-file .env.prod pull

# 后台启动服务
docker compose -f docker-compose.prod.yml --env-file .env.prod up -d
```

### 5. 验证访问
在云平台安全组（防火墙）放行端口（默认 `8080`）。
- 读者门户：`http://云服务器公网IP:8080/library`
- 管理后台：`http://云服务器公网IP:8080/library/admin`
- 默认管理员：`admin` / `admin123`
- 默认读者：`R20240001` / `reader123`

---

## 常用运维命令

- **查看容器运行状态**：
  ```bash
  docker compose -f docker-compose.prod.yml --env-file .env.prod ps
  ```
- **查看应用实时运行日志**：
  ```bash
  docker compose -f docker-compose.prod.yml --env-file .env.prod logs -f app
  ```
- **版本更新迭代（更新镜像后重新部署）**：
  ```bash
  docker compose -f docker-compose.prod.yml --env-file .env.prod pull
  docker compose -f docker-compose.prod.yml --env-file .env.prod up -d
  ```
- **停止服务**：
  ```bash
  docker compose -f docker-compose.prod.yml --env-file .env.prod down
  ```
