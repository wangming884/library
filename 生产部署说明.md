# 云端生产环境部署指南 (Docker Hub + 已有 MySQL)

本方案用于在**云服务器（如阿里云、腾讯云、华为云、AWS 等）**上一键拉取 Docker Hub 镜像并运行，复用服务器上已安装的 MySQL 数据库。
当前配置默认使用 **7623** 端口。

---

## 阶段一：在本地打包并推送到 Docker Hub

> **前提**：您需要在 [Docker Hub](https://hub.docker.com/) 注册账号，并拥有一个仓库（例如 `leemingze/library-management`）。

在**本地开发机**项目根目录终端执行：

### 1. 登录 Docker Hub
```bash
docker login
# 输入您的 Docker Hub 用户名和密码 (或 Access Token)
```

### 2. 构建镜像并打标签
```bash
# 格式：docker build -t 您的用户名/仓库名:标签 .
docker build -t leemingze/library-management:v1.0.0 .
```

### 3. 推送镜像至 Docker Hub
```bash
docker push leemingze/library-management:v1.0.0
```

---

## 阶段二：云服务器数据库准备（若未初始化）

如果云服务器上的 MySQL 数据库尚未创建图书库或数据表：

1. 登录服务器 MySQL：
   ```bash
   mysql -u root -p
   ```
2. 创建数据库：
   ```sql
   CREATE DATABASE IF NOT EXISTS library_db DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
   ```
3. 导入初始表结构和预置数据：
   将项目中的 `sql/schema.sql` 和 `sql/data.sql` 上传至服务器执行：
   ```bash
   mysql -u root -p library_db < sql/schema.sql
   mysql -u root -p library_db < sql/data.sql
   ```

---

## 阶段三：云服务器部署运行

在**云端服务器**上只需保留两个配置文件即可轻量启动：

### 1. 创建应用目录
```bash
mkdir -p /opt/library && cd /opt/library
```

### 2. 上传配置文件
将以下 2 个文件上传到云服务器的 `/opt/library`：
- `docker-compose.prod.yml`
- `.env.prod`（或从 `.env.prod.example` 复制）

### 3. 配置生产环境变量
编辑 `.env.prod`：
```bash
nano .env.prod  # 或使用 vim 编辑
```

配置要点：
- `DOCKER_IMAGE=leemingze/library-management:v1.0.0`
- `APP_PORT=7623`（服务监听端口）
- `DB_HOST=127.0.0.1`（因使用 host 网络模式直连本机 MySQL，直接填 127.0.0.1 即可，无需开放公网访问权限）
- `DB_PORT=3306`
- `DB_PASSWORD=您的云端MySQL真实密码`

### 4. 启动容器
```bash
# 停止并清理可能存在的旧容器
docker compose -f docker-compose.prod.yml --env-file .env.prod down

# 启动服务
docker compose -f docker-compose.prod.yml --env-file .env.prod up -d
```

### 5. 验证访问
请确保在**云平台控制台（阿里云/腾讯云等）的安全组**放行 TCP **7623** 端口。
- 读者门户：`http://云服务器公网IP:7623/library`
- 管理后台：`http://云服务器公网IP:7623/library/admin`
- 默认管理员：`admin` / `admin123`
- 默认读者：`R20240001` / `reader123`

---

## 常见问题与排查指南

### 1. 启动日志报 `Failed to create server shutdown socket on port [8005]: Address already in use`
- **原因**：由于使用了 `host` 网络模式直连宿主机 MySQL，Tomcat 启动后默认尝试在 `localhost:8005` 监听关闭指令。如果云服务器宿主机此前运行过本地 Tomcat 或其他服务占用了 8005 端口，会导致 Tomcat 误判冲突而退出。
- **解决方案**：
  最新的 `docker-compose.prod.yml` 已经内置了启动命令处理（将 Tomcat 8005 关机端口设为 `-1` 禁用）。直接使用最新 `docker-compose.prod.yml` 即可避免该冲突。

### 2. 检查 7623 端口是否正常监听
在云服务器上执行：
```bash
# 查看 7623 端口监听状态
netstat -tunlp | grep 7623
# 或者
ss -tulpn | grep 7623
```
若看到 `java` 或 `docker` 处于 `LISTEN` 状态，说明服务已成功在该端口运行。

---

## 常用运维命令

- **查看容器运行状态**：
  ```bash
  docker compose -f docker-compose.prod.yml --env-file .env.prod ps
  ```
- **查看应用实时运行日志**：
  ```bash
  docker compose -f docker-compose.prod.yml --env-file .env.prod logs -f app
  ```
- **版本更新迭代（更新镜像后重新部署）**：
  ```bash
  docker compose -f docker-compose.prod.yml --env-file .env.prod pull
  docker compose -f docker-compose.prod.yml --env-file .env.prod up -d
  ```
- **停止服务**：
  ```bash
  docker compose -f docker-compose.prod.yml --env-file .env.prod down
  ```

---

## 阶段四：宝塔面板配置域名与自动跳转到 /library

如果您的域名为 `library.oneais.dev`，容器映射在服务器的 `7623` 端口上，希望用户访问 `https://library.oneais.dev/` 时**自动无缝跳转**到 `https://library.oneais.dev/library`：

### 1. 配置反向代理（让域名转发到 7623 端口）
1. 打开**宝塔面板** -> 点击左侧【**网站**】。
2. 找到您的网站 `library.oneais.dev`，点击右侧【**设置**】。
3. 点击左侧【**反向代理**】->【**添加反向代理**】：
   - **代理名称**：`library-proxy`（可自定义）
   - **目标 URL**：`http://127.0.0.1:7623`
   - **发送域名**：`$host`
   - **代理目录**：`/`
4. 点击【**保存**】。

### 2. 配置根路径自动跳转（访问 / 自动跳转到 /library/）
因为 Tomcat 应用部署在 `/library` 路径下，如果用户直接在浏览器输入顶级域名访问根路径 `/`，需要让 Nginx 自动重定向到 `/library/`。

**最推荐且最稳妥的做法（修改站点配置文件）**：
1. 在网站设置窗口中，点击左侧【**配置文件**】。
2. 找到 `server { ... }` 块内部，在已有反向代理规则的前面，添加以下精确重定向规则：
   ```nginx
   # 根路径访问自动 302 重定向到 /library/
   location = / {
       return 302 /library/;
   }
   ```
3. 点击【**保存**】。Nginx 会自动重新加载生效。

**效果测试**：
- 用户在浏览器输入 `https://library.oneais.dev` 或 `https://library.oneais.dev/` 时，将自动瞬间跳转到 `https://library.oneais.dev/library/`，直接展现图书馆全新首页！
- 后续的图书检索、空间选座、文献榜单、个人中心等路由均可正常解析。
